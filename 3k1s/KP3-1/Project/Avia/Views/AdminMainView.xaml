<Window x:Class="Avia.Views.AdminMainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Панель администратора" 
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="Выход" Command="{Binding LogoutCommand}"/>
        </Menu>

        <TabControl Grid.Row="1" SelectedIndex="0"
                    SelectionChanged="TabControl_SelectionChanged">

            <TabItem Header="Пользователи" Tag="Users">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
                        <Button Content="Создать" Command="{Binding CreateUserCommand}" Margin="5"/>
                        <Button Content="Редактировать" Command="{Binding EditUserCommand}" Margin="5"/>
                        <Button Content="Удалить" Command="{Binding DeleteUserCommand}" Margin="5"/>
                    </StackPanel>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding Users}" 
                              SelectedItem="{Binding SelectedUser}"
                              AutoGenerateColumns="False" Margin="10">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding UserId}" Width="50"/>
                            <DataGridTextColumn Header="Паспорт" Binding="{Binding PassportNumber}" Width="120"/>
                            <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}" Width="100"/>
                            <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}" Width="100"/>
                            <DataGridTextColumn Header="Отчество" Binding="{Binding MiddleName}" Width="100"/>
                            <DataGridTextColumn Header="Роль" Binding="{Binding AccessRole}" Width="80"/>
                            <DataGridTextColumn Header="Дата рождения" Binding="{Binding BirthDate, StringFormat=dd.MM.yyyy}" Width="120"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="Рейсы" Tag="Flights">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
                        <Button Content="Создать" Command="{Binding CreateFlightCommand}" Margin="5"/>
                        <Button Content="Редактировать" Command="{Binding EditFlightCommand}" Margin="5"/>
                        <Button Content="Удалить" Command="{Binding DeleteFlightCommand}" Margin="5"/>
                    </StackPanel>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding Flights}" 
                              SelectedItem="{Binding SelectedFlight}"
                              AutoGenerateColumns="False" Margin="10">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding FlightId}" Width="50"/>
                            <DataGridTextColumn Header="Откуда" Binding="{Binding DepartureCity}" Width="100"/>
                            <DataGridTextColumn Header="Куда" Binding="{Binding ArrivalCity}" Width="100"/>
                            <DataGridTextColumn Header="Дата вылета" Binding="{Binding DepartureDate, StringFormat=dd.MM.yyyy}" Width="100"/>
                            <DataGridTextColumn Header="Время вылета" Binding="{Binding DepartureTime, StringFormat=hh\\:mm}" Width="100"/>
                            <DataGridTextColumn Header="Авиакомпания" Binding="{Binding Airline}" Width="120"/>
                            <DataGridTextColumn Header="Эконом цена" Binding="{Binding EconomyPrice}" Width="100"/>
                            <DataGridTextColumn Header="Бизнес цена" Binding="{Binding BusinessPrice}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="Билеты" Tag="Tickets">
                <DataGrid ItemsSource="{Binding Tickets}" 
                          SelectedItem="{Binding SelectedTicket}"
                          AutoGenerateColumns="False" Margin="10">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding TicketId}" Width="50"/>
                        <DataGridTextColumn Header="Пользователь" Binding="{Binding User.LastName}" Width="120"/>
                        <DataGridTextColumn Header="Рейс" Binding="{Binding Flight.DepartureCity}" Width="100"/>
                        <DataGridTextColumn Header="Класс" Binding="{Binding ClassType}" Width="80"/>
                        <DataGridTextColumn Header="Багаж" Binding="{Binding Baggage}" Width="60"/>
                        <DataGridTextColumn Header="Дата покупки" Binding="{Binding PurchaseDate, StringFormat=dd.MM.yyyy HH:mm}" Width="150"/>
                        <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>

