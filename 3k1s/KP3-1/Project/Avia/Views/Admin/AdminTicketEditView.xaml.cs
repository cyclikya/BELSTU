namespace Avia.Views.Admin;

/// <summary>
/// Окно для добавления и редактирования билетов.
/// Используется как для создания новых билетов (Add), так и для редактирования существующих (Edit).
/// </summary>
public partial class AdminTicketEditView : System.Windows.Window
{
    public AdminTicketEditView()
    {
        InitializeComponent();
    }
}

